apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "allday-frontend-helm.fullname" . }}-test-connection"
  labels:
    {{- include "allday-frontend-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "allday-frontend-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
